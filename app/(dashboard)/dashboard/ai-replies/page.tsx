export const dynamic = "force-dynamic";

import { AIRepliesPage } from "@/components/dashboard/ai-replies-page";

export const metadata = { title: "AI Replies — ReviewPilot" };

export default function AIRepliesRoute() {
  return <AIRepliesPage />;
}
