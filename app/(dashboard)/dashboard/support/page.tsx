export const dynamic = "force-dynamic";
export const metadata = { title: "Support — Reviewdot.in" };

import { SupportPage } from "@/components/support/support-page";

export default function Page() {
  return <SupportPage />;
}
